let guess_of_number = 0
game.splash("Guess my number")
let random = game.askForNumber("What number is your guess? (1 to 6)")
if (guess_of_number < 6) {
    game.splash("Please enter a valid number")
    game.reset()
} else if (false) {
	
}
